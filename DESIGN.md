# DESIGN.md — القرار البصري المقفول

> هذا الملف هو **المصدر الوحيد** لكل قيمة بصرية في المشروع.
> ممنوع أي قيمة لون أو خط أو مقاس في الكود لا تعود لهنا عبر `src/design/tokens.ts`.
> تعديل التصميم يبدأ من هذا الملف، لا من ملف CSS.

---

## 0. مصدر القرار

مرجع البرييف كان كرت XO من بوت `Swip`، وبُني منه أول تخطيط للتحقق من البنية.
ثم صدر قرار صريح بالابتعاد عن التقليد: **الأحمر والأصفر هما لونا الهوية**،
مع حذف أي شعار أو watermark، وزيادة ثقل الظلال والحروف.

ما بقي من المرجع هو **المبدأ** لا الشكل: عالم القصاصات الورقية المقصوصة.
وما تغيّر هو كل ما يُرى: اللوحة، وعلاقة الخلفية بالأسطح، والظل، والأشكال.

**نقطة الافتراق الحاسمة:** في المرجع الظل أسود دائمًا. عندنا **لون الظل متغيّر**،
والعنصر الأصفر يُلقي ظلًا أحمر. هذا هو موضع اندماج لوني الهوية، ولا يوجد
في المرجع ولا في أي بوت يستخدم نفس أسلوب الملصقات.

---

## 1. الموضوع (Subject)

بوت ديسكورد عربي عام يدير ألعابًا جماعية سريعة داخل قنوات السيرفر.
جمهوره: شباب خليجي/عربي في سيرفرات صوتية، يلعبون جولات قصيرة بين بعضهم.
وظيفة كل صورة واحدة فقط: **أن يعرف اللاعب فورًا وضعه الآن وماذا يفعل بعدها** — من داخل الصورة، بلا قراءة نص طويل.

---

## 2. المفردات (Vernacular)

عالم المرجع ليس "واجهة تطبيق"، بل **ملصقات ورقية مقصوصة ولوحة ملعب أطفال**:

- ورق كرتوني باهت اللون بدل الأبيض الرقمي
- كل قصاصة لها **حد أسود سميك** كأنها قُصّت بمقص وأُلصقت
- **ظل صلب بلا تدرّج** — ظل ملصق مرفوع عن الورقة، لا ظل CSS ناعم
- غيوم مسطّحة وأشكال blob عضوية — لغة لوحات الملاعب والكتب المصوّرة
- الأقواس `« »` كعلامة عنوان — من تقليد الطباعة العربية لا من الويب

هذه المفردات هي **المصدر المشروع الوحيد** لأي قرار بصري جديد.
أي عنصر لا يمكن تبريره بأنه "قصاصة ورقية مقصوصة" لا يدخل التصميم.

---

## 3. اللوحة (Palette)

لونان فقط يحملان الهوية — الأحمر والأصفر — وكل ما عداهما ورق وحبر.

| التوكن | القيمة | الدور |
|---|---|---|
| `paper` | `#EFE3CB` | رمل دافئ — خلفية المشهد. **أعمق** من الأسطح فتطفو البطاقات فوقه |
| `paperTint` | `#E6D8BC` | كتل الخلفية العضوية وحدها. لا يحمل نصًا أبدًا |
| `surface` | `#FBF5E8` | كريمي فاتح — سطح البطاقات |
| `ink` | `#1A1512` | أسود دافئ — كل حد وكل نص |
| `red` | `#E03A2F` | لون الهوية الأول: الشريط، العناوين، اسم اللعبة، زر الانضمام |
| `redDeep` | `#A8231A` | **ظل** العناصر الصفراء — موضع اندماج اللونين |
| `yellow` | `#FBBF1E` | لون الهوية الثاني: الفعل البطل، حلقة القائد، عدّاد اللاعبين |
| `cream` | `#FFFDF7` | النص فوق الأحمر، وخلفية الأفتار |

**قاعدة اللون:** لا لون جديد بلا سطر في هذا الجدول. التدرّجات ممنوعة نهائيًا — كل الألوان مسطّحة.

**عكس علاقة المرجع مقصود:** عندهم البطاقة أعمق من الخلفية، وعندنا أفتح.
هذا يرفع تباين النص ويجعل البطاقات تطفو بدل أن تغوص.

**التباين:** `ink` على `surface` ≈ 16:1. النص الفاتح (`cream`) يُستخدم **فقط** فوق
`red` و `redDeep` و `ink`. فوق `yellow` يكون النص `ink` دائمًا — الأصفر فاتح ولا يحمل نصًا فاتحًا.

---

## 4. الطباعة (Type)

| الدور | الخط | لماذا |
|---|---|---|
| Display | `Baloo Bhaijaan 2` | عربي هندسي مدوّر ثقيل، أقرب متاح بترخيص OFL لعناوين المرجع |
| Body | `Cairo` | عربي معاصر واضح بأوزان متعددة، ينسجم مع Display بلا منافسة |
| Latin | `Baloo Bhaijaan 2` (يحمل لاتينيًا مدمجًا) | انسجام لا تطابق؛ نفس الوزن البصري داخل الجملة المختلطة |

**المقياس** (بعرض مشهد 1500px):

| التوكن | الحجم | الاستخدام |
|---|---|---|
| `display` | 82px | اسم اللعبة — أثقل وأكبر بكثير من المرجع ليحمل المشهد وحده بعد حذف الشعار |
| `title` | 38px | عناوين الأقسام «...» |
| `body` | 26px | الشرح والوصف |
| `label` | 25px | أسماء اللاعبين، نصوص الأزرار |
| `meta` | 21px | العدّادات، شريط الحالة |

**اسم اللعبة أحمر بحدّ أسود** (`-webkit-text-stroke: 2px`): الحرف العربي يحتاج
ثقلًا إضافيًا ليصمد أمام الحدود السميكة حوله، والحد يمنع الأحمر من الاهتزاز على الكريمي.

**قواعد إلزامية للعربية** (كسرها عيب تسليم):
- `letter-spacing: 0` — أي تباعد يفصل الحروف ويكسر الوصل
- `line-height` لا يقل عن `1.7`
- ممنوع `text-transform: uppercase`
- `dir="rtl"` على الجذر مرة واحدة، و `text-align: start` لا `right`
- logical properties فقط (`margin-inline-start`) — لا `left` / `right`
- الأرقام: عربية غربية `0-9` موحّدة في كل الواجهة بلا استثناء

---

## 5. التوقيع (Signature)

**الظل الملوّن.** كل عنصر — بطاقة، زر، أفتار، شريط — له حد أسود `5px` وظل صلب
بلا blur، **ولون الظل يحمل معنى**:

| العنصر | الظل | لماذا |
|---|---|---|
| بطاقة عادية، زر أحمر، شريط | `ink` | البنية محايدة |
| البطاقة البطلة، الزر الأصفر | `redDeep` | الأصفر فوق الأحمر = اندماج لوني الهوية |

الظل ثقيل عمدًا (`11px` للبطاقات، `7px` للأزرار) — أثقل من المرجع بنحو الثلث.
اتجاهه يسار-أسفل لأنه يتبع اتجاه القراءة RTL: الضوء من جهة بداية السطر.

**لا شعار ولا watermark في أي مشهد.** الهوية تُحمل باللون والظل والشكل، لا باسم
مطبوع. هذا قرار مزدوج الفائدة: يبقي التصميم صالحًا مهما كان الاسم لاحقًا،
ويترك الصورة للعبة نفسها بدل أن يزاحمها الشعار.

**عناصر التوقيع المساندة:**
- الأقواس `« »` حول كل عنوان قسم، **باللون الأحمر** لا بلون النص
- كتل عضوية (blobs) للعمق الخلفي، وكإطار تجلس فيه أيقونة كل لعبة — لا مربعات
- حلقة صفراء حول أفتار قائد اللعبة
- شريط حالة يقول للاعب كم ينقص للبدء، فلا يبقى فراغ بلا معلومة

---

## 6. استثناء موثّق — نص عربي داخل صورة

قاعدة "ممنوع نص عربي داخل صورة" مرفوعة هنا **بطلب صريح من البرييف**:
مبدأ المنتج نفسه أن كل مخرجاته صور مركّبة.

**التخفيف إلزامي ولا يُتجاوز:** كل حالة حرجة — قائمة اللاعبين، الوقت المتبقي،
النتيجة، الفائز — تُنسخ أيضًا في **نص رسالة ديسكورد** بجانب الصورة.
الفائدة ثلاثية: قابل للنسخ والبحث، يعمل مع قارئات الشاشة، ويبقى مفهومًا لو فشل الرندر.

---

## 7. ما هو ممنوع في هذا المشروع

- أي تدرّج لوني (`gradient`) — اللوحة كلها مسطّحة
- أي ظل ناعم (`blur` في `box-shadow`) — الظل صلب دائمًا
- `backdrop-filter` / زجاجية
- خطوط النظام (`Segoe UI`, `Tahoma`) — تعرض العربية مختلفة على كل جهاز
- مستطيل مليء بالنص كشاشة كاملة — الشاشة مشهد لا مستند
- أي لون خارج جدول القسم 3
- اسم أو شعار مطبوع داخل المشهد
- كتلة عضوية تُقصّ عند حافة المشهد — تُقرأ قرصًا لا شكلًا؛ إمّا داخل التصميم كليًا أو لا توضع
