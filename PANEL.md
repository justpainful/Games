# مِقْوَد — تطبيق التحكّم

تطبيق SwiftUI أصلي على الآيفون، يتحكّم ببوت Games كاملًا، ويعمل على شبكتك
وحدها: بلا Tailscale، وبلا خادم في الإنترنت، وبلا اشتراك.

## كيف يشتغل

```
الآيفون  ──── واي فاي البيت ────►  الحاسب: عمليّة البوت، منفذ 4590
   └── يجد الجهاز وحده عبر Bonjour، بلا أن يُكتب عنوان
```

خادم التحكّم يعيش داخل عمليّة البوت نفسها، فلا شيء إضافي يُشغَّل. ويعلن نفسه
على الشبكة باسم `_miqwad._tcp`، والتطبيق يسمع النداء ويصل. تغيير الراوتر
لتوزيع العناوين لا يعني شيئًا، وهو ما كان يفرض Tailscale في النسخة السابقة.

### البناء والتثبيت

لا ماك في الصورة. `‎.github/workflows/miqwad.yml` يبني على رَنَر macOS في
GitHub Actions: يولّد مشروع Xcode بـXcodeGen، يشغّل الاختبارات على محاكي، ثم
يخرج **IPA غير موقّعة** كـartifact. تُثبَّت بـAltStore أو Sideloadly ويوقّعها
جهازك بشهادة مجانية.

### تسجيل الدخول

بابان، وكلاهما يعطي نفس الجلسة:

1. **رمز اقتران** — ستّة أرقام تُطبع في نافذة تشغيل البوت. يُدخل مرّة، ويُحفظ
   رمز الجلسة في الـKeychain ثلاثين يومًا.
2. **ديسكورد** — نفس تسجيل الدخول الحالي (OAuth)، ولا يفتح إلا لمالك البوت
   (`BOT_OWNER_ID`).

الأول هو الأساس هنا: ديسكورد لا يقبل عنوان عودة غير مسجَّل حرفيًا في بوابة
المطوّرين، وعنوان الجهاز داخل الشبكة يتغيّر. ومن يقرأ الرمز في الطرفيّة جالسٌ
أمام الحاسب، وهو تعريف المالك في شبكة محلية.

## الشرائح

كل شريحة تنتهي بشيء **يعمل** على الجوال: يُبنى في CI، ويُثبَّت، ويُستعمل.

| # | الشريحة | ما يصير شغّالًا | الحالة |
|---|---------|------------------|--------|
| 1 | الوصل والحالة | يجد الجهاز، يقترن، يعرض حالة البوت والجلسات الجارية حيّة | ✅ |
| 2 | السيرفر والألعاب | البادئة، القنوات، الرتب، وتشغيل كل لعبة وإطفاؤها | ✅ |
| 3 | مقابض الألعاب | عدد الجولات ومدّتها، معلَنةً من اللعبة ومقروءةً في التشغيل | ✅ |

> **الشريحة الثالثة بدأت من المحرّك لا من الشاشة**، لأن عمود
> `GameConfig.settings` كان يُكتب فيه ولا تقرؤه لعبة واحدة. صار المقبض يُعلَن
> في `games/define.ts` باسمه وحدّيه ووحدته، ويُقرأ في `games/typing.ts` عبر
> `table.tune`، ويُحصر في مداه عند **القراءة** لا عند الكتابة وحدها — لأن ما
> في القاعدة اليوم كُتب قبل وجود هذا الحد.
>
> والباقي لهذي الشريحة: صورة اللعبة المخصّصة، والمصرَّح لهم، ومقابض ألعاب
> الأدوار (مافيا وروليت وكودنيمز) — وهي تحتاج مثل ما احتاجت ألعاب الكتابة:
> إعلانًا في التعريف وقراءةً في التشغيل قبل أي شاشة.
| 4 | النقاط واللاعبون | الصدارة، بحث عضو، إعطاء وخصم وتصفير، سجل المباريات | ⬜ |
| 5 | المتجر والمفاتيح | المفاتيح، الأسعار، دائمة أم مستهلكة، مخزون كل لاعب | ⬜ |
| 6 | الجلسات الحيّة | إيقاف لعبة، بدء لعبة في قناة من الجوال، بثّ حيّ للأحداث | ⬜ |
| 7 | هويّة البوت والنظام | الحالة والنشاط والاسم والصورة، السجلّات، الكلمات، النسخ الاحتياطي | ⬜ |
| 8 | الجرد الأخير | مراجعة «ما نقص شيء» بندًا بندًا مقابل كل ما في المشروع | ⬜ |

## أين يعيش الكود

```
src/panel/            خادم التحكّم داخل عمليّة البوت
  serve.ts            المنفذ والإقلاع
  mount.ts            المسارات تحت /ctl
  session.ts          رمز الاقتران ورمز الجلسة
  discovery.ts        إعلان Bonjour
  status.ts           حالة البوت الحيّة
  net.ts              عناوين الجهاز للإدخال اليدوي

ios/Miqwad/           التطبيق
  Net/                الاكتشاف والعميل والحالة والـKeychain
  Features/           الشاشات
  Assets.xcassets/    الأيقونة
ios/MiqwadTests/      اختبارات
```

`src/api/` و`ios/Games/` يخصّان تطبيق اللاعبين ولا يتغيّران. والمشترك بين
التطبيقين هو `ios/Games/Design/` وحده: لون واحد وخط واحد للسطحين.

الداشبورد القديم في `src/dashboard/` يبقى عاملًا حتى تكتمل الشريحة الثالثة، ثم
يُطفأ. حذفه قبل ذلك يترك فجوة تحكّم بلا بديل.

## التشغيل

```bash
npm start
```

خادم التحكّم يقلع مع البوت بلا إعداد ولا متغيّر بيئة، ويطبع رمز الاقتران
والعناوين. المنفذ 4590 افتراضًا، ويُبدَّل بـ`PANEL_PORT`.
