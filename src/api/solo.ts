/**
 * ما يمكن بدؤه من التطبيق بلاعب واحد.
 *
 * `GameDef.players.min` لا يصلح مقياسًا هنا: كل الألعاب مكتوب فيها `min: 2`
 * لأنه **قاعدة لوبي ديسكورد** («لا تبدأ سباقًا بشخص واحد»)، لا قيدًا في المحرّك.
 * ألعاب الكتابة تدور بلاعب واحد بلا تعديل حرف: الحلقة تعرض سؤالًا، تنتظر
 * إجابة، ثم تنهي بالترتيب — ولا شيء فيها يفترض وجود خصم.
 *
 * ولذلك القائمة صريحة ومكتوبة باليد بدل أن تُشتقّ باستنتاج هشّ. ما ليس فيها
 * يحتاج لاعبين حقيقيين (إكس أو، مافيا، الكراسي…) أو إقصاءً بينهم (`kt`)،
 * وذلك ينتظر اللوبي الجماعي عبر WebSocket وهو غير منفّذ بعد.
 *
 * إضافة لعبة كتابة جديدة تتطلب سطرًا هنا. النقص يظهر كغياب اللعبة من التطبيق
 * لا كعطل صامت، وهذا أهون الضررين.
 */
export const SOLO_KEYS: ReadonlySet<string> = new Set([
  // مبنية على `typing()` المشتركة حرفيًا
  'aalam',
  'aks',
  'arqam',
  'asraa',
  'awasim',
  'event',
  'fakek',
  'hisab',
  'jam3',
  'maarifa',
  'mufrad',
  'qara',
  'rakeb',
  // حلقات خاصة بنفس الشكل: مشهد ← انتظار إجابة ← ترتيب نهائي
  'huroof',
  'khammen',
])

export function isSoloable(key: string): boolean {
  return SOLO_KEYS.has(key)
}
